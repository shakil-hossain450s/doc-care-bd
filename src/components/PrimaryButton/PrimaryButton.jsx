const PrimaryButton = ({btnText}) => {
  return (
    <button className="btn bg-[#176AE5] border-none text-white rounded-full px-8">
      {btnText}
    </button>
  );
};

export default PrimaryButton;